% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add.R
\name{add}
\alias{add}
\title{Add packages to the project}
\usage{
add(
  packages,
  dev = FALSE,
  bioc = FALSE,
  do_lock = TRUE,
  do_install = TRUE,
  no_binary = FALSE,
  bin = NULL,
  dir = NULL,
  cache_dir = NULL,
  progress = NULL,
  quiet = FALSE
)
}
\arguments{
\item{packages}{Character vector of package names (e.g. \code{c("ggplot2", "tidymodels@>=1.0.0", "user/repo@main")}).}

\item{dev}{If \code{TRUE}, add as dev dependencies.}

\item{bioc}{If \code{TRUE}, packages come from Bioconductor.}

\item{do_lock}{If \code{TRUE}, update the lockfile with the added package(s).}

\item{do_install}{If \code{TRUE}, install the added package(s).
Ignored if \code{do_lock} is \code{FALSE}.}

\item{no_binary}{If \code{TRUE}, build everything from source instead of
using pre-built binaries — an escape hatch for a binary that does not
suit the host.}

\item{bin}{Optional path to the uvr binary. If \code{NULL}, searches common locations using \code{\link{find_uvr}}.}

\item{dir}{Optional working directory. Defaults to \code{getwd()}.}

\item{cache_dir}{Path to cache directory. Defaults to `NULL`, which uses
the `UVR_CACHE_DIR` environment variable if set, or `"~/.uvr/cache/"` otherwise.}

\item{progress}{Control uvr's progress display for this call:
\code{"always"} forces spinners and progress bars even though output is
piped (they are otherwise hidden because R captures uvr's output, so
uvr never sees a TTY), \code{"never"} hides them. The default
\code{NULL} leaves the \code{UVR_PROGRESS} environment variable in
charge.}

\item{quiet}{If \code{TRUE}, suppress output.}
}
\value{
A character vector of output lines (returned invisibly).
  On failure, throws an error with the exit code.
}
\description{
Adds one or more packages to the manifest, updates the lockfile, and
installs them. Equivalent to \code{uvr add} on the command line.
}
\examples{
\dontrun{
add("ggplot2") # add to toml, lock, install
add("dplyr", do_lock = FALSE) # no lock, no install
add("dplyr", do_install = FALSE) # lock, no install
add("tidymodels@>=1.0.0")
add("user/repo@main")
add(c("ggplot2", "tidymodels@>=1.0.0", "user/repo@main"))
add(c("DESeq2", "GenomicRanges"), bioc = TRUE) # bioconductor
}
}
\seealso{
Other package managers:
\code{\link[=cache_clean]{cache_clean()}},
\code{\link[=export]{export()}},
\code{\link[=import]{import()}},
\code{\link[=lock]{lock()}},
\code{\link[=remove_pkgs]{remove_pkgs()}},
\code{\link[=scan]{scan()}},
\code{\link[=sync]{sync()}},
\code{\link[=tree]{tree()}},
\code{\link[=update_pkgs]{update_pkgs()}}
}
\concept{package managers}
