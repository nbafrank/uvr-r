% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/init.R
\name{init}
\alias{init}
\title{Initialize a new uvr project}
\usage{
init(
  name = NULL,
  r_version = NULL,
  bin = NULL,
  dir = NULL,
  lib_dir = NULL,
  quiet = FALSE
)
}
\arguments{
\item{name}{Optional project name. Defaults to the current directory name.}

\item{r_version}{Optional R version constraint, e.g. \code{">=4.3.0"}.}

\item{bin}{Optional path to the uvr binary. If \code{NULL}, searches common locations using \code{\link{find_uvr}}.}

\item{dir}{Optional working directory. Defaults to \code{getwd()}.}

\item{lib_dir}{Path to the library directory where to install packages. Defaults
to `NULL`, which uses the `UVR_LIBRARY` environment variable if set, or
the project-level `".uvr/library/"` otherwise.}

\item{quiet}{If \code{TRUE}, suppress output.}
}
\value{
A character vector of output lines (returned invisibly).
  On failure, throws an error with the exit code.
}
\description{
Creates a \code{uvr.toml} manifest and \code{.uvr/library/} directory.
Also generates \code{.Rprofile} code to link to the \code{.uvr/library/} on session start.
Equivalent to \code{uvr init} on the command line.
}
\examples{
\dontrun{
init()
init(name = "my-project", r_version = ">=4.3.0")
}
}
\seealso{
Other uvr setup functions:
\code{\link[=completions]{completions()}},
\code{\link[=doctor]{doctor()}},
\code{\link[=install_uvr]{install_uvr()}},
\code{\link[=update_uvr]{update_uvr()}}
}
\concept{uvr setup functions}
