% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scan.R
\name{scan}
\alias{scan}
\title{Find packages your code uses but the manifest does not declare}
\usage{
scan(all = FALSE, bin = NULL, dir = NULL, quiet = FALSE)
}
\arguments{
\item{all}{If \code{TRUE}, list every package found rather than only the
ones missing from the manifest.}

\item{bin}{Optional path to the uvr binary. If \code{NULL}, searches common locations using \code{\link{find_uvr}}.}

\item{dir}{Optional working directory. Defaults to \code{getwd()}.}

\item{quiet}{If \code{TRUE}, suppress output.}
}
\value{
A character vector of output lines (returned invisibly).
  On failure, throws an error with the exit code.
}
\description{
Scans the project's \code{.R}, \code{.Rmd} and \code{.qmd} files for
\code{library()}, \code{require()} and \code{pkg::fun()} usage, and reports
anything missing from \code{uvr.toml}. Equivalent to \code{uvr scan}.
}
\details{
Useful straight after \code{\link{import}}, or on a project that predates
uvr, to catch dependencies nobody wrote down.
}
\examples{
\dontrun{
scan()             # what's used but undeclared?
scan(all = TRUE)   # everything the scan found
}
}
\seealso{
Other package managers:
\code{\link[=add]{add()}},
\code{\link[=cache_clean]{cache_clean()}},
\code{\link[=export]{export()}},
\code{\link[=import]{import()}},
\code{\link[=lock]{lock()}},
\code{\link[=remove_pkgs]{remove_pkgs()}},
\code{\link[=sync]{sync()}},
\code{\link[=tree]{tree()}},
\code{\link[=update_pkgs]{update_pkgs()}}
}
\concept{package managers}
