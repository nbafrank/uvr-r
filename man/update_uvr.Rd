% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/update.R
\name{update_uvr}
\alias{update_uvr}
\title{Update uvr (R package + CLI binary)}
\usage{
update_uvr(ref = "HEAD", method = "auto", quiet = FALSE)
}
\arguments{
\item{ref}{Git ref (branch, tag, commit SHA) to install the R package from.
Defaults to the default branch (\code{"HEAD"}).}

\item{method}{Installation method: \code{"auto"} (default) tries a pre-built
GitHub release binary first, then falls back to building from source via
\code{cargo install}. \code{"binary"} downloads a pre-built binary only.
\code{"cargo"} builds from source only.}

\item{quiet}{If \code{TRUE}, suppress progress messages.}
}
\value{
Invisible named list with \code{r_package} (installed version or
  \code{NA} on failure) and \code{binary} (path to the CLI binary).
}
\description{
Convenience helper that updates both the \code{uvr} R companion package
and the underlying \code{uvr} CLI binary in one call.
}
\details{
The function does \emph{not} automatically restart your R session. After
it completes, run \code{rstudioapi::restartSession()} (RStudio / Positron),
or restart R to load the newly installed package.
}
\examples{
\dontrun{
update_uvr()
# then restart R to pick up the new package
}
}
