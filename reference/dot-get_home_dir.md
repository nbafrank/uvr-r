# Return HOME/USER directory

Internal helper that returns the user's home directory. (Windows:
`USERPROFILE`, Unix: `HOME`)

## Usage

``` r
.get_home_dir()
```

## Value

Path to user's home directory.
