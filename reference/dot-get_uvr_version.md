# Get uvr binary version

Get uvr binary version

## Usage

``` r
.get_uvr_version(bin = NULL)
```

## Arguments

- bin:

  Optional path to the uvr binary. If `NULL`, searches common locations
  using [`find_uvr`](find_uvr.md).

## Value

character value of uvr binary version (i.e. "1.0.0")
