# Create a temporary directory and cleanup this calls parent environment closes

Create a temporary directory and cleanup this calls parent environment
closes

## Usage

``` r
.make_temp_dir(name = "uvr-test", envir = parent.frame())
```
