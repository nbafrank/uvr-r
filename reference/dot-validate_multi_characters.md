# Validate multiple (more than 0) character value(s)

Validate multiple (more than 0) character value(s)

## Usage

``` r
.validate_multi_characters(character_flags, null_ok = FALSE)
```

## Arguments

- character_flags:

  Named list of values to validate
